import { 
  clientSchema, 
  clientCreateSchema, 
  clientReturnSchema, 
  clientReadSchema, 
  clientUpdateSchema, 
  clientContactsRetrieveSchema 
} from "./client.schema";
import sessionSchema from "./session.schema";
import { 
  contactSchema, 
  contactCreateSchema, 
  contactReturnSchema, 
  contactReadSchema 
} from "./contact.schema";

export {
  clientSchema,
  clientCreateSchema,
  clientReturnSchema,
  clientReadSchema,
  sessionSchema,
  clientUpdateSchema,
  clientContactsRetrieveSchema ,
  contactSchema,
  contactCreateSchema,
  contactReturnSchema,
  contactReadSchema
};